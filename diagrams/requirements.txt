# Dependencies for diagrams/generate.py (architecture diagram generation).
#
# Install into a SEPARATE virtualenv from the main app -- do not combine with
# the root requirements.txt. aws-pdk pins cdk-nag<3.0.0, which conflicts with
# the cdk-nag v3 used by the main CDK app (see ../requirements.txt), and pip's
# resolver will fail if both are installed together in one environment.
#
# Usage:
#   python3 -m venv .venv-diagrams && source .venv-diagrams/bin/activate
#   pip install -r diagrams/requirements.txt
#   python diagrams/generate.py
aws-cdk-lib==2.260.0
constructs==10.6.0
aws-pdk==0.26.15
